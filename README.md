
  # AIReady (Copia)

  This is a code bundle for AIReady (Copia). The original project is available at https://www.figma.com/design/rApeLJz0u9OrtCArNNKWeY/AIReady--Copia-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  